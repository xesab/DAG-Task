services:
  web:
    image: dag-task:latest
    ports:
      - "8200:8000"
    volumes:
      - db_data:/app/db
    env_file:
      - .env
    container_name: dag-task
    restart: unless-stopped

volumes:
  db_data:
    driver: local